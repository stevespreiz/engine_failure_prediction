from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Machine learning approach to predicting aircraft engine failure in an open NASA dataset.',
    author='Steven Spreizer',
    license='',
)
